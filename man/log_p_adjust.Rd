% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_p_adjust.R
\name{log_p_adjust}
\alias{log_p_adjust}
\alias{.log_bonferroni}
\alias{.log_holm}
\alias{.log_hommel}
\alias{.log_hochberg}
\alias{.log_BH}
\alias{.log_BY}
\title{Multiple Test Correction (Log-Space)}
\usage{
.log_bonferroni(p, n, base)

.log_holm(p, n, lp, base)

.log_hommel(p, n, lp, base)

.log_hochberg(p, n, lp, base)

.log_BH(p, n, lp, base)

.log_BY(p, n, lp, base)
}
\arguments{
\item{p}{Numeric vector of p-values in \strong{log-space}.}

\item{n}{Integer. Total number of tests used for correction.}

\item{base}{Integer. Logarithmic base to use for transformations.}

\item{lp}{Integer. Effective length of the p-value vector (Not used in Bonferroni).}
}
\value{
A numeric vector of \strong{adjusted p-values in log-space}.
}
\description{
These are internal functions that implement multiple test correction methods in \strong{log-space}.
They accept log-transformed p-values and return adjusted p-values in
log-space using different correction methods.
}
\details{
The following methods are provided:
\itemize{
\item \code{.log_holm} — Holm (1979)
\item \code{.log_hochberg} — Hochberg (1988)
\item \code{.log_hommel} — Hommel (1988)
\item \code{.log_BH} — Benjamini & Hochberg (1995)
\item \code{.log_bonferroni} — Bonferroni correction
\item \code{.log_BY} — Benjamini & Yekutieli (2001)
}
}
\examples{
pvals <- log(c(0.01, 0.02, 0.05))

# Bonferroni
.log_bonferroni(pvals, n = length(pvals), base = exp(1))

# Other methods (all use the same implementation)
.log_BH(pvals, n = length(pvals), lp = length(pvals), base = exp(1))

}
\seealso{
\link[stats:p.adjust]{stats::p.adjust} for the linear-scale equivalents.
}
